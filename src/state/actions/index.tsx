export const depositMoney = () => (dispatch: any) => {
    dispatch({
        type: "desposte",
        // payload: amount
    })
}

export const widthDrawMoney = () => {
    return (dispatch: any) => {
        dispatch({

        })
    }
}